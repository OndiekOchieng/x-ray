import type { ProvenanceCluster } from '@/lib/types'

export function ProvenanceClusterView({ cluster }: { cluster: ProvenanceCluster }) { return <section className="rounded-2xl border border-border bg-card p-5 md:p-7"><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-muted-foreground">Provenance</p><div className="mt-5 flex flex-col items-center gap-3"><div className="rounded-lg border border-primary/40 bg-primary/5 px-4 py-3 text-center text-sm font-semibold">{cluster.source}</div><div className="h-5 w-px bg-border"/><div className="grid w-full gap-2 sm:grid-cols-3">{cluster.publications.map((publication) => <div key={publication} className="rounded-lg border border-border p-3 text-center text-xs text-foreground">{publication}</div>)}</div></div><p className="mt-5 text-sm text-muted-foreground">{cluster.publications.length} publications · 1 apparent originating dataset</p><p className="mt-2 text-xs leading-relaxed text-muted-foreground">{cluster.note}</p></section> }
