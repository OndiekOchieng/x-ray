import type { ExplorerClaim } from '@/lib/types'

export function FindingPanel({ claim }: { claim: ExplorerClaim }) {
  const finding = claim.finding
  return <section className="rounded-2xl border border-border bg-card p-5 md:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-muted-foreground mb-3">{claim.origin === 'DISCOVERED' ? 'Discovered claim' : 'Surface claim'}</p><h1 className="max-w-3xl text-2xl md:text-3xl font-semibold leading-tight text-foreground">{claim.text}</h1></div><div className="rounded-lg border border-primary/30 bg-primary/5 px-3 py-2 text-right"><p className="font-mono text-[10px] uppercase tracking-wider text-primary">Finding</p><p className="mt-1 text-sm font-semibold text-foreground">{finding.status}</p><p className="font-mono text-[10px] text-muted-foreground">{Math.round(finding.confidence * 100)}% confidence</p></div></div><div className="mt-7 grid gap-5 md:grid-cols-2"><div><p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">What X-Ray can establish</p><p className="mt-2 text-base leading-relaxed text-foreground">{finding.establish}</p></div><div><p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Why</p><p className="mt-2 text-sm leading-relaxed text-muted-foreground">{finding.rationale}</p></div></div><div className="mt-7 border-t border-border pt-5"><p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">What would change this finding?</p><p className="mt-2 text-sm text-foreground">{finding.wouldChange}</p></div></section>
}
