'use client'

import { useMemo, useState } from 'react'
import Link from 'next/link'
import { ArrowRight, Clock3, Search, History } from 'lucide-react'
import { AppShell } from '@/components/layout/app-shell'
import { cachedInvestigations, libraryDescription, libraryPrinciple, versionHistory } from '@/lib/types/gaps'

export function InvestigationLibrary() {
  const [query, setQuery] = useState('')
  const [showHistory, setShowHistory] = useState(false)
  const results = useMemo(() => cachedInvestigations.filter((item) => `${item.title} ${item.source}`.toLowerCase().includes(query.toLowerCase())), [query])
  return <AppShell><div className="mx-auto max-w-5xl px-4 py-10 sm:px-6 lg:px-8"><header className="border-b border-border pb-10"><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary">Library · cached public X-Rays</p><div className="mt-4 flex flex-wrap items-end justify-between gap-6"><div><h1 className="text-4xl font-semibold tracking-tight md:text-5xl">Investigations, indexed.</h1><p className="mt-4 max-w-2xl text-lg text-muted-foreground">{libraryDescription}</p></div><p className="max-w-xs text-right font-serif text-xl italic text-foreground/80">{libraryPrinciple}</p></div></header><main className="py-10"><div className="relative max-w-xl"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search cached investigations" className="w-full rounded-xl border border-border bg-card py-3 pl-10 pr-4 text-sm outline-none focus:border-primary" aria-label="Search cached investigations" /></div><div className="mt-8 grid gap-5">{results.map((item) => <article key={item.id} className="rounded-2xl border border-border bg-card p-6"><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="font-mono text-[10px] uppercase tracking-widest text-primary">{item.id} · {item.protocol}</p><h2 className="mt-3 text-2xl font-semibold">{item.title}</h2><p className="mt-2 max-w-2xl text-muted-foreground">{item.summary}</p></div><Link href={`/xray/${item.id}`} className="inline-flex items-center gap-2 rounded-lg bg-foreground px-4 py-2 text-sm font-semibold text-background">Explore X-Ray <ArrowRight className="size-4" /></Link></div><div className="mt-7 grid gap-4 border-t border-border pt-5 text-xs text-muted-foreground sm:grid-cols-2 lg:grid-cols-5"><span><b className="block text-foreground">Surface source</b>{item.source}</span><span><b className="block text-foreground">Investigated</b>{item.investigatedAt}</span><span><b className="block text-foreground">Claims</b>{item.claims}</span><span><b className="block text-foreground">Receipts</b>{item.receipts}</span><span><b className="block text-foreground">Open gaps</b>{item.gaps}</span></div><div className="mt-5 flex flex-wrap items-center justify-between gap-3 border-t border-border pt-4"><button onClick={() => setShowHistory(!showHistory)} className="inline-flex items-center gap-2 text-xs text-muted-foreground hover:text-foreground"><History className="size-4" /> {showHistory ? 'Hide version history' : 'Version 1 · Investigated Sep 13, 2026'}</button><span className="inline-flex items-center gap-2 text-xs text-muted-foreground"><Clock3 className="size-4" /> Last updated {item.updatedAt}</span></div>{showHistory && <div className="mt-4 rounded-xl bg-muted/40 p-4 text-sm text-muted-foreground"><p>New receipts can trigger re-evaluation. Earlier versions remain part of the record.</p><ul className="mt-3 flex flex-col gap-2 font-mono text-xs">{versionHistory.map((version) => <li key={version}>{version}</li>)}</ul></div>}</article>)}{results.length === 0 && <div className="rounded-2xl border border-dashed border-border p-10 text-center text-sm text-muted-foreground">No cached investigation matches this search.</div>}</div><p className="mt-10 max-w-2xl text-sm leading-6 text-muted-foreground">Public claims change. New documents emerge. X-Ray keeps the receipts and lets investigations improve instead of pretending a verdict is permanent.</p></main></div></AppShell>
}
