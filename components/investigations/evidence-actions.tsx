import Link from 'next/link'
import type { ExplorerClaim } from '@/lib/types'

export function GapPreview({ claim }: { claim: ExplorerClaim }) { if (!claim.gap) return null; return <section className="rounded-2xl border border-primary/30 bg-primary/5 p-5 md:p-7"><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary">Evidence gap</p><h2 className="mt-3 text-lg font-semibold text-foreground">{claim.gap.title}</h2><div className="mt-5 grid gap-5 md:grid-cols-2"><div><p className="text-xs uppercase tracking-wider text-muted-foreground">Why it matters</p><p className="mt-2 text-sm leading-relaxed text-foreground">{claim.gap.whyItMatters}</p></div><div><p className="text-xs uppercase tracking-wider text-muted-foreground">What would settle it</p><p className="mt-2 text-sm leading-relaxed text-foreground">{claim.gap.whatWouldSettleIt}</p></div></div><Link href="/gap/GAP-001" className="mt-6 inline-block text-sm font-semibold text-primary hover:underline">See how to resolve this gap →</Link></section> }

export function EvidenceShareCard({ claim, investigationDate }: { claim: ExplorerClaim; investigationDate: string }) { return <section className="rounded-2xl border border-border bg-background p-5"><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary">X-RAY · EVIDENCE CARD</p><p className="mt-5 text-lg font-semibold leading-snug text-foreground">{claim.text}</p><p className="mt-3 text-xs font-semibold uppercase tracking-wider text-muted-foreground">{claim.finding.status}</p><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{claim.finding.establish}</p><div className="mt-5 flex items-center justify-between gap-3 border-t border-border pt-4 font-mono text-[10px] text-muted-foreground"><span>Investigated {investigationDate}</span><span>Receipts available</span></div></section> }
