'use client'

import type { EvidenceReceipt } from '@/lib/types'

export function ReceiptDrawer({ receipt, onClose }: { receipt?: EvidenceReceipt; onClose: () => void }) {
  if (!receipt) return null
  return <div className="fixed inset-0 z-50 flex justify-end bg-black/30" onClick={onClose}><aside role="dialog" aria-label="Evidence receipt" className="h-full w-full max-w-md overflow-y-auto border-l border-border bg-background p-6 shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary">Evidence receipt</p><h2 className="mt-2 text-xl font-semibold text-foreground">{receipt.title}</h2></div><button onClick={onClose} className="text-sm text-muted-foreground hover:text-foreground" aria-label="Close receipt">Close</button></div><div className="mt-8 flex flex-col gap-5 text-sm"><div><p className="text-xs uppercase tracking-wider text-muted-foreground">Evidence proposition</p><p className="mt-2 leading-relaxed text-foreground">{receipt.proposition}</p></div><div><p className="text-xs uppercase tracking-wider text-muted-foreground">Relationship</p><p className="mt-2 text-muted-foreground">{receipt.relationship}</p></div><dl className="grid grid-cols-2 gap-4 border-y border-border py-5"><div><dt className="text-xs text-muted-foreground">Institution</dt><dd className="mt-1 text-foreground">{receipt.institution}</dd></div><div><dt className="text-xs text-muted-foreground">Published</dt><dd className="mt-1 font-mono text-xs text-foreground">{receipt.publishedAt}</dd></div><div><dt className="text-xs text-muted-foreground">Source type</dt><dd className="mt-1 text-foreground">{receipt.sourceType}</dd></div><div><dt className="text-xs text-muted-foreground">Classification</dt><dd className="mt-1 text-foreground">{receipt.classification} · {receipt.provenance}</dd></div><div><dt className="text-xs text-muted-foreground">Strength</dt><dd className="mt-1 text-foreground">{receipt.strength}</dd></div></dl><a href={receipt.url} target="_blank" rel="noreferrer" className="text-sm font-semibold text-primary hover:underline">View original source ↗</a></div></aside></div>
}
