import type { Discrepancy } from '@/lib/types'

export function DiscrepancyCard({ discrepancy }: { discrepancy: Discrepancy }) { return <article className="rounded-xl border border-border bg-card p-5"><div className="flex flex-wrap items-center justify-between gap-2"><h3 className="font-semibold text-foreground">{discrepancy.title}</h3><span className="font-mono text-[10px] uppercase tracking-wider text-muted-foreground">{discrepancy.resolution}</span></div><p className="mt-2 text-xs font-semibold uppercase tracking-wider text-primary">{discrepancy.classification}</p><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{discrepancy.summary}</p><div className="mt-5 grid gap-2 sm:grid-cols-2"><div className="rounded-lg bg-muted/40 p-3 text-xs text-foreground">{discrepancy.leftLabel}</div><div className="rounded-lg bg-muted/40 p-3 text-xs text-foreground">{discrepancy.rightLabel}</div></div></article> }
