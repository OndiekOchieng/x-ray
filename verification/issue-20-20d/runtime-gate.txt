=== pnpm check:live-runtime ===
ok    1 · an unconfigured environment composes nothing and says which slots
ok    2 · a partial configuration composes nothing — no silent fallback
ok    3 · a complete configuration composes, and registration is conditional
ok    3b · every required slot corresponds to a registered capability
ok    4 · re-evaluation is not planned by the live runtime
ok    5 · discovery is followed by retrieval, in that order
ok    6 · a record that could not be obtained stays unreadable, and stays
ok    7 · search-only material is never readable to TRACE
ok    8 · the composed runtime drives every stage end to end
ok    9 · the stage owns every canonical decision
ok    10 · a handle the stage never issued is ignored
ok    11 · PROVENANCE reports a gap rather than deciding lineage
ok    12 · a blocked stage is a gap, not a failure, and not an empty success
ok    13 · no submitted URL blocks every stage with a reason
ok    14 · a paused search turn continues and completes, without restarting
ok    15 · a continued search feeds the composed runtime as material
ok    16 · the continuation bound stops the turn and says so
ok    20 · a live reviewer is actually asked, and its checks become EVALUATED
ok    21 · an unconfigured reviewer is a truthful blocker, never a silent pass
ok    22 · a refusing reviewer leaves the check unevaluated, not flagged
ok    23 · a broken reviewer is an outage, and blocks rather than committing
ok    24 · no provider-specific reviewer type leaves composition
ok    25 · a record with no retrievedAt gets X-Ray's own observation time
ok    17 · the stage layer is provider-neutral and the pipeline is untouched
ok    18 · review stays out of the pipeline adapters
ok    19 · no secret reaches a composed runtime, a plan, or a graph

26/26 live runtime checks passed
Every call went to 127.0.0.1. No civic URL was used.

=== earlier gates ===

28/28 Anthropic adapter checks passed
29/29 Anthropic retrieval checks passed

=== the reviewer chain, as it now runs ===
composeLiveRuntime            -> constructs the ReviewerModel
registerLiveProviders         -> installs it via setReviewerModelProvider
                                 (its own seam: StageAdapters has no reviewer member,
                                  because a research stage must not be handed one)
GraduationService.assess      -> collectModelJudgments(candidate, model)
                              -> assessGraduation(candidate, { model, judgments })
                              -> reviewXRayGraph(graph, { validation, model, judgments })
                                 ... which is pure, and no network call happens inside it

Before the amendment: assess() passed `model` into the SYNCHRONOUS reviewXRayGraph,
which does not collect judgments. A fully configured deployment reported the
capability and left every model-assisted check NOT_EVALUATED.

reviewer absent   -> model-assisted checks NOT_EVALUATED with a reason;
                     graduation BLOCKED; fullCapability false. No PASS exists to
                     collapse into: ReviewOutcome is EVALUATED | NOT_EVALUATED.
reviewer refuses  -> the asked checks stay NOT_EVALUATED with a reason; no review
                     finding and no graduation reason about the graph
reviewer throws   -> an outage, not a capability gap; propagates, no assessment is
                     appended, so commit() has nothing to stand on

Requirements <-> registrations:
  REQUIRED_SLOTS = RESEARCH_MODEL, REVIEWER_MODEL, RETRIEVAL
  research model + retrieval reach the pipeline through the runtime's StageAdapters
  reviewer reaches the REVIEW gate and graduation through its own seam
  -> every required slot has a consumer; none is a dead composition token

=== retrievedAt ===
observedAt(provided, now):
  a provider value that is a real ISO instant  -> used
  anything else, or absent                     -> the injected clock, now()
Before: `document.retrievedAt ?? ctx.correlation.investigationId` — an investigation
id where a timestamp belongs. It typechecked because both are strings, and every
fixture supplied a retrievedAt so the fallback never ran.
Verified backstop: with the bad fallback restored, INGEST fails
STRUCTURAL/NON_ISO_TIMESTAMP and no source is minted. The bug was latent, not
silent-corrupting — but it would have failed any live run where a provider omitted
the timestamp.

=== negative controls (each verified to apply, then reverted) ===
W  composes despite missing slots                    -> FAIL 1, 2, 3
X  registration no longer conditional                -> FAIL 3
Y  search results passed through without retrieval   -> FAIL 5, 6, 8, 15
Z  unobtained records dropped after retrieval        -> FAIL 6, 7
AA TRACE accepts evidence from an unobtained record  -> FAIL 7
     (STAGED validation then catches it as XR-INV-006/EVIDENCE_FROM_UNOBTAINED_SOURCE)
AB INGEST asserts originStatus before PROVENANCE     -> FAIL 9
AC continuation restarts instead of resuming         -> FAIL 14

amendment controls:
AD graduation stops collecting judgments             -> FAIL 20, 22, 23
     "judge() was never called" — this is the original defect, reproduced
AE assessGraduation drops the judgments it was given -> FAIL 20
     "all 6 model-assisted checks are still NOT_EVALUATED"
AF the reviewer seam is never installed              -> FAIL 3, 3b
     3b: "REVIEWER_MODEL is required but never installed"
AG retrievedAt falls back to a non-timestamp         -> FAIL 25
     the run fails STRUCTURAL/NON_ISO_TIMESTAMP at INGEST; verified directly
