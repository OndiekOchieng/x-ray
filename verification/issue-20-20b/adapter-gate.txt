=== pnpm check:anthropic-adapters ===
ok    1 · both adapters implement their ports and nothing wider
ok    2 · the registry composes both adapters from configuration alone
ok    3 · a well-formed answer becomes typed proposals
ok    4 · an empty answer is accepted, not treated as a failure
ok    5 · every malformed shape is rejected before it becomes a proposal
ok    6 · a required vocabulary is checked in every slot that has one
ok    7 · XR-INV-007 is enforced at the boundary: a finding must be reversible
ok    8 · a handle the stage did not offer is rejected
ok    9 · no canonical id ever reaches the provider
ok    10 · a provider cannot assert what a stage owns
ok    11 · HTTP failures map onto the documented error semantics
ok    11b · both forms of 429 are transient, and neither diagnoses billing
ok    11b2 · a missing header alone can never produce EXHAUSTED
ok    11c · a 400 spend limit is EXHAUSTED; an ordinary 400 is PERMANENT
ok    11d · investigated material cannot make a failure look like a spend limit
ok    11e · classification never reads the raw body, only typed fields
ok    12 · a refusal is a capability fact, not evidence about the claim
ok    13 · a truncated answer is transient; a wrong-shaped one is not
ok    14 · the adapter never retries; the pipeline owns that
ok    15 · the key reaches a header and nothing else
ok    16 · a provider message is never echoed back, only its type
ok    17 · diagnostics are recorded off the graph and never in a proposal
ok    18 · a reviewer judgment is validated and keeps its targets
ok    19 · a reviewer cannot flag an artifact it was not shown
ok    20 · an unanswered check stays unanswered
ok    21 · every reviewer query kind has an authored question
ok    22 · the request is a forced tool call carrying the cutoff
ok    23 · an unobtained document is presented as unobtained

28/28 Anthropic adapter checks passed
Every call went to 127.0.0.1. No request left this machine.

=== pnpm check:providers ===
ok    stop line · prompts stay inside the prompt module

20/20 provider composition checks passed

=== failure mapping (final) ===
signal read                                     outcome                  reason / disposition
----------------------------------------------  -----------------------  --------------------
401  |  authentication_error                    CapabilityUnavailable    NOT_CONFIGURED
403  |  permission_error                        CapabilityUnavailable    NOT_CONFIGURED
404  |  not_found_error   (no such model)       CapabilityUnavailable    NOT_CONFIGURED
402  |  billing_error                           CapabilityUnavailable    EXHAUSTED
413  |  request_too_large                       CapabilityUnavailable    REFUSED_FOR_INPUT
       stop_reason: refusal                     CapabilityUnavailable    REFUSED_FOR_INPUT
400 invalid_request_error + trusted spend msg   CapabilityUnavailable    EXHAUSTED
400 invalid_request_error, anything else        AdapterFailure           PERMANENT
429 rate_limit_error  WITH retry-after          AdapterFailure           TRANSIENT  (+ the stated delay)
429 rate_limit_error  WITHOUT retry-after       AdapterFailure           TRANSIENT  (+ what to check if it persists)
408, 409                                        AdapterFailure           TRANSIENT
500, 502, 503, 504, 529  |  api_error,
  overloaded_error, timeout_error               AdapterFailure           TRANSIENT
network error / timeout                         AdapterFailure           TRANSIENT
stop_reason: max_tokens                         AdapterFailure           TRANSIENT
any other 4xx                                   AdapterFailure           PERMANENT
200 prose / wrong tool / non-JSON               AdapterFailure           PERMANENT
malformed or invalid structured answer          AdapterFailure           PERMANENT
a handle that was not offered                   AdapterFailure           PERMANENT

Signals are read in order of how far they can be trusted:
  1. HTTP status, where it decides alone            (401, 402, 403, 404, 413)
  2. the documented error.type, filtered to a closed vocabulary
  3. documented headers, for what they POSITIVELY state. A header's absence is
     never read as a diagnosis: it can be stripped by a proxy or unset by the
     edge that answered.
  4. error.message — ONE condition only (400 spend limit), under two guards:
       the message must be <= 400 characters, and must contain no 24-character
       run that also appears in the request we sent.

EXHAUSTED requires a POSITIVE billing signal — 402 / billing_error, or a
trusted spend-limit statement. It is never inferred from something absent.

=== negative controls (each reverted immediately) ===
All re-run against the final classifier. Control C (an earlier "a rate limit
stops being transient") and control G ("every 429 treated as ordinary rate
limiting") are both retired: G describes what the code now correctly does, so
it can no longer be a control. J replaces them.

A  presentClaim leaks the canonical id                → FAIL 9   (classify: a canonical id was sent to the provider)
B  the decoder stops checking the offered scope       → FAIL 8   (an invented claim handle: was accepted)
D  the key is put in the prompt                       → FAIL 15  (the key was in the request body)
E  a reviewer may target an artifact it was not shown → FAIL 19  (an id that was not in the query: was accepted)
F  back to phrase-matching the whole response body    → FAIL 11c, 11d, 11e
     11c  a genuine spend message was missed entirely
     11d  "an echoed 400 did not fail"
     11e  "classification still reads the raw body"
H  the 402 billing branch removed                     → FAIL 11, 11b2
     11   402 became PERMANENT
     11b2 a 429 carrying billing_error stopped being EXHAUSTED — which is the
          positive-signal path doing the work, not the header
I  the echo guard removed                             → FAIL 11d ("an echoed 400 did not fail")
J  a missing retry-after inferred to be a spend cap   → FAIL 11b, 11b2
     11b   "no retry-after: did not fail"
     11b2  "bare 429: produced EXHAUSTED from no positive signal"
     J is the defect this final amendment removes.
